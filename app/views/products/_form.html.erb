<%= form_for @product, :html => { :class => 'form-horizontal', :method => :post }, :url => { :action => :create } do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation" class="alert alert-error">
      <h4>Please fix the following <%= pluralize(@product.errors.count, "error") %> and try again:</h4>
      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name %>
    </div>
  </div>
  <div class="field control-group">
    <%= f.label :description, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_area :description, :rows => '3' %>
    </div>
  </div>
  <div class="field control-group">
    <%= f.label :source, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :source %>
    </div>
  </div>
  <div class="field control-group">
    <%= f.label :artist, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :artist %>
    </div>
  </div>
  <div class="field control-group">
    <%= f.label :image, :class => 'control-label' %>
    <div class="controls">
      <%= f.file_field :image %>
    </div>
  </div>
  <div class="field control-group">
    <%= f.label :your_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :referer %>
    </div>
  </div>
  <div class="field control-group">
    <%= f.label :your_contact_info, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :contact %>
      <small><samp>(twitter or email)</samp></small>
    </div>
  </div>
  <div class="actions control-group">
    <div class="controls">
      <%= f.submit 'Submit', :class=> 'btn btn-primary'%>
    </div>
  </div>
<% end %>
